# PTools 版本历史

本文档记录 PTools 各版本的主要功能与变化。

## 1.4.2（2026-09-20）

### 修复

- 修复顺序粘贴在 `Ctrl + Shift + V` 尚未释放时就模拟系统粘贴，导致目标编辑器可能忽略粘贴按键的问题；现在会等待快捷键释放后再恢复目标应用并执行粘贴。
- 增加全局快捷键按下、释放和重复事件的状态控制，避免长按或异常事件顺序造成重复粘贴和重复出队。

## 1.4.1（2026-09-19）

### 修复

- 修复顺序粘贴使用 `Ctrl + Shift + V` 时只更新系统剪贴板、没有粘贴到用户当前光标所在应用的问题；现在会恢复原目标应用并发送真实的系统粘贴快捷键。
- 修复 Windows 下顺序粘贴目标窗口刷新和焦点恢复问题，避免 PTools 自身窗口被误识别为粘贴目标。
- 修复顺序粘贴队列拖拽排序在 WebView 先触发 `dragend`、再触发 `drop` 的事件顺序下失效的问题。

## 1.4.0（2026-09-18）

### 新增

- 粘贴面板顶部中央新增快捷工具栏，最多展示 5 个工具；默认提供文本对比、顺序粘贴、JSON 格式化、URL 编解码和 Base64 编解码，并可替换为 XML、HTML 或日期转换。
- 新增快捷工具管理面板，支持点击添加或移除、拖拽排序；默认使用 `cmd+1` 至 `cmd+5` 按当前位置启动工具，排序后映射自动更新，也可在设置中逐项修改快捷键。
- 重构文本格式化注册机制，新增 XML、HTML、URL、Base64 和日期识别及对应工具；文本卡片右键菜单的“使用工具”子菜单可打开全部文本工具。
- 新增文本对比工作区：连续选择两张文本或文本文件卡片并按 `D`，或使用右键菜单，即可打开独立的左右对比窗口；多文件卡片支持先选择具体文件。
- 新增空白文本对比入口：通过快捷工具栏可打开左右均为空、默认置顶的窗口，自行粘贴或输入内容。
- 对比支持原文及 JSON/JSONC、XML、HTML 格式化模式，可自动识别或手动指定类型；支持转义 JSON，XML/HTML 会保留有意义的空白。
- 对比窗口提供行内差异高亮、同步滚动、差异跳转、交换左右、自动换行、折叠相同区域、导入 UTF-8 文本文件、恢复原文和复制当前内容。
- 对比窗口左右内容均可直接编辑并即时更新差异，保留光标及撤销/重做记录；编辑只作用于临时副本，不修改原文件或剪贴板历史。
- 粘贴面板右上角新增三点菜单，当前可直接打开设置界面。
- 新增顺序粘贴临时队列：采集模式自动收集之后复制的文本、图片和文件，允许重复内容，支持拖拽排序、单项移除和清空，不会从现有历史卡片导入内容。
- 顺序粘贴支持正序和倒序出队；默认使用可配置的全局快捷键 `Ctrl + Shift + V`，仅在粘贴模式且队列非空时注册。
- 顺序粘贴成功后才移除当前项并高亮下一项，失败时保留内容并关联显示错误；队列窗口始终保持可见，关闭窗口或退出应用后清空临时状态。

### 优化

- 快捷工具采用跟随主题的轻量彩色图标；悬停约 100 ms 后显示工具名称和当前快捷键，已打开的工具显示激活状态，拖拽过程中提供明确的目标位置反馈。
- 区分快捷工具的点击与卡片快捷键入口：点击顶部格式化工具打开空白工作区，不读取剪贴板内容；在卡片上使用工具快捷键时才导入当前选中内容。
- 快捷键设置新增冲突校验，禁止快捷工具之间，或与面板唤醒、标签切换、卡片切换及搜索快捷键重复；1.4.0 开发阶段保存过的 `Alt + 1～5` 配置会迁移为新的 `cmd+1`～`cmd+5` 默认值。
- 格式化工具栏移动到编辑区顶部并压缩为单行“图标 + 快捷键”布局，操作按钮悬停时显示完整说明，成功反馈改为工具栏内的轻量状态提示。
- 所有格式化工具统一提供键盘操作：`Ctrl + R` 格式化，`Ctrl + B` 切换自动换行；JSON 使用 `Ctrl + Q/W/A/S/T` 执行去注释、压缩、转义及格式转换，URL、Base64 和日期使用 `Ctrl + E/D` 转换。
- 格式化、编码和解码会优先处理编辑器选区；未选择内容时才处理全文。编辑器搜索栏调整到顶部并完成中文本地化，第一次按 `Esc` 仅关闭搜索。
- 对比窗口支持多开、实时跟随主题和置顶，失焦不会关闭；`Command + D` / `Alt + D` 切换置顶，未置顶时可按 `Esc` 关闭，关闭有临时修改的窗口前会确认。
- 完整格式化和文本对比移至本地后台执行并支持取消；直接编辑采用增量差异计算，减少长文本计算对界面的阻塞。
- 提升历史记录滚动性能：继续按每页 20 条查询，在距离末尾约 1.5 屏时预加载；列表仅渲染视口附近卡片，长文本使用轻量预览，图片按需加载，数据库查询移至后台线程。
- 重构剪贴板卡片为轻量原生风格，统一圆角、头部、选中边界和主题变量；移除悬停位移动画，避免卡片阴影被裁切或状态切换造成布局跳动。
- 对比来源使用位于卡片头部右侧的 `Diff-L` / `DIFF-R` 标记，不改变卡片尺寸；标记可点击取消，并在打开对比或关闭粘贴面板后清空。
- 操作错误改为在对应卡片底部短暂显示红色提示，未关联具体卡片的错误才使用面板级提示，避免遮挡内容或丢失操作上下文。
- 重做卡片右键菜单，使用不透明、跟随主题的分组样式；标签和工具收纳为子菜单，优化悬停反馈、菜单间距、屏幕边缘定位，并在完成操作后自动关闭。
- macOS 粘贴面板支持点击应用外部区域自动关闭；重新唤起面板时会重新计算卡片视口、重置首张卡片选择并刷新快捷工具状态，关闭时同步收起菜单并清除临时对比标记。
- 普通历史卡片改用受控的原生剪贴板写入，避免采集模式将用户从现有历史恢复或粘贴的内容重新加入顺序队列。

### 修复

- 修复转义 JSON 在首尾或结构间包含真实换行、回车和 Tab 缩进时无法识别或格式化的问题，同时保留字符串中的原有转义字符。
- 修复长文本只有局部不一致时，后续相同 JSON、XML 或代码行仍被连续标记为差异的问题；直接编辑后的增量对比同样使用改进后的对齐逻辑。
- 修复隐藏后重新唤起粘贴面板时，虚拟列表可能沿用无效宽度并出现卡片重叠的问题。
- 修复面板快捷键监听抢先拦截格式化编辑器 `Command + F` 的问题，编辑器搜索和快捷工具快捷键现在可以同时正常工作。
- 修复格式化窗口搜索栏位置、中文显示和双层 `Esc` 行为不一致的问题。

## 1.3.0（2026-08-31）

### 新增

- 新增 Windows 平台支持，可在 Windows 上使用剪贴板历史、搜索、标签、主题和 JSON 格式化等功能。
- Windows 默认使用 `Alt + V` 唤出粘贴面板，并支持在设置中自定义全局快捷键。
- Windows 支持自动粘贴：唤出面板时记录原应用窗口，选择历史内容后恢复窗口焦点并发送 `Ctrl + V`。
- Windows 支持左键点击系统托盘图标打开设置界面。

### 优化

- 新增统一的平台适配层，将 macOS、Windows 和通用窗口逻辑按平台拆分，保持各系统原生交互行为。
- 根据 Windows 屏幕缩放比例和鼠标所在显示器动态调整粘贴面板的位置与高度，改善多显示器和高分屏体验。
- 优化 Windows 下格式化窗口的打开、聚焦、拖动、固定和失焦关闭行为。
- Windows 无需 macOS 辅助功能权限，设置界面会根据当前系统自动显示适用的权限说明。
- 将 Tauri 启动脚本改为跨平台实现，支持在 macOS 和 Windows 开发环境中使用统一命令。

## 1.2.0（2026-08-20）

### 新增

- 自动识别剪贴板历史中的 JSON 和 JSONC 文本，选中卡片后可按 `F` 打开格式化窗口。
- 新增带行号、语法高亮和代码折叠的 JSON 编辑器，支持重新格式化、折叠全部、展开全部和去除注释。
- 支持压缩复制、压缩转义复制，以及将 JSON 转换为 XML 或 TypeScript 后复制。
- 格式化窗口支持书钉固定；固定后保持置顶，取消固定时失焦自动隐藏。
- 格式化窗口新增左上角关闭按钮，支持同时打开多个相互独立的格式化窗口。
- 支持在格式化窗口中使用 `Command + D`（兼容 `Alt + D`）切换当前窗口固定状态。

### 优化

- 文本格式化功能采用可扩展格式注册机制，为后续支持 XML、HTML 等文本格式预留统一入口。
- 格式化窗口完整适配柔光灰、经典浅色和深色主题，并实时跟随设置变化。
- 格式化窗口打开期间仍可使用全局快捷键唤出粘贴面板。
- 优化格式化多窗口层级：新窗口置前并错开显示，窗口组内部切换不会误关闭未固定窗口。
- 修复固定格式化窗口从其他应用返回后首次点击无法立即操作的问题。

## 1.1.0（2026-08-17）

### 新增

- 在粘贴面板中新增历史内容搜索框。
- 支持搜索全部本地历史数据，并可与内容格式、标签筛选组合使用。
- 支持按 `Command + F` 快速聚焦搜索框。
- 支持在搜索框按回车直接复制或粘贴当前选中结果；未选择结果时使用第一条。
- 设置界面新增主题配色，可在柔光灰、经典浅色和深色主题之间切换并自动保存。
- 支持配置是否显示文本、图片和文件类型筛选标签，默认保持隐藏。
- 支持通过可配置快捷键循环切换粘贴面板标签，默认使用 `Ctrl + Q` 向左、`Ctrl + E` 向右。
- 粘贴面板最左侧新增快捷键帮助入口，展示当前配置的面板、标签和卡片操作说明。
- 支持通过可配置快捷键 `Ctrl + A` / `Ctrl + D` 切换左右卡片，同时支持左右方向键。

### 优化

- 历史记录改为数据库分页查询，首次仅加载 20 条。
- 横向滚动接近列表末尾时按需加载下一页，降低大量历史记录对界面性能的影响。
- 搜索输入自动触发查询，并通过防抖减少频繁访问数据库。
- 关闭粘贴面板时自动清空搜索内容，确保下次打开时从完整历史开始浏览。
- 搜索框聚焦时支持 macOS 原生的撤销、剪切、复制、粘贴和全选快捷键。
- 快捷键帮助改为独立的非激活窗口，在粘贴面板上方展示，避免遮挡卡片、扩展背景和窗口缩放闪烁，并移除不必要的滚动条。
- 关闭或重新唤起粘贴面板时自动收起快捷键帮助并清除 `?` 按钮焦点，避免入口被误选中。
- 为卡片添加或移除标签时仅更新当前卡片，避免列表刷新后跳回第一项。
- 标签筛选结果按照标签添加时间倒序排列，最近添加标签的卡片优先展示。
- 卡片右键菜单将标签收纳到悬停展开的子菜单，并在标签变更后自动关闭。
- 优化 macOS DMG 安装窗口，增加品牌背景、紧凑布局和拖拽安装指引。
- 历史更新事件不再传输全部记录，避免一次性加载大量图片数据。
- 建立应用级主题变量，设置界面、粘贴面板、右键菜单和标签编辑器统一跟随主题变化，并支持后续新增界面复用。
- 加深柔光灰主题的中性灰背景，使整体颜色更接近 macOS 原生界面。
- 深色主题同步适配 macOS 原生窗口标题栏和系统控件外观。

### 修复

- 修复历史记录超过数量上限后带标签内容可能被自动删除的问题；带标签记录不再受保留期限和数量上限清理，数量上限仅统计未添加标签的记录。

## 1.0.0（2026-08-12）

### 新增

- 支持记录和查看文本、图片及文件剪贴板历史。
- 支持通过全局快捷键呼出粘贴面板，默认快捷键为 `Ctrl + V`。
- 支持双击、回车和数字键 `1`–`5` 快速恢复或粘贴历史内容。
- 支持创建自定义标签，并按内容格式或标签筛选历史记录。
- 支持配置记录类型、保留时间、最大历史数量和自动粘贴。
- 支持查看和申请 macOS 辅助功能权限。
- 支持从设置界面检查软件更新。
- 剪贴板历史和应用设置仅保存在本机。
