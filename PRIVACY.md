# 隐私说明

## 本地数据

PTools 将设置保存为 `settings.json`，并将启用类型的剪贴板历史保存为本机 `history.sqlite` 数据库。文本、图片和文件路径都可能属于敏感信息；应用提供保留时间、记录类型、最大数量和清空历史设置。

默认设置为保留 30 天、最多 200 条，并记录文本、图片和文件。用户可以在设置页面关闭任意记录类型、改为更短保留期，或清空全部历史。

## 系统权限

自动粘贴需要 macOS 允许应用模拟键盘输入。该权限仅用于在用户明确选择一条历史记录后发送 `Cmd+V`，不会用于后台输入或监控其他应用。

## 网络与文件访问

当前应用没有上传剪贴板历史的功能。为展示被复制图片文件的缩略图，主面板会通过 asset 协议读取所选历史条目关联的本地文件；不会主动遍历、扫描或上传用户文件。
